% !TEX root = ../main.tex
\chapter{THIẾT KẾ VÀ HIỆN THỰC}
\thispagestyle{mainstyle}

Dựa trên kết quả phân tích ở Chương~3, chương này trình bày cách hệ thống được hiện thực cụ thể: từ thiết kế cơ sở dữ liệu, kiến trúc phần mềm đến giao diện người dùng và kết quả kiểm thử. Toàn bộ hệ thống được xây dựng trên nền tảng NestJS~\cite{nestjs-docs}, Next.js~\cite{nextjs-docs} và React Native~\cite{reactnative-docs}.

\section{Sơ đồ lớp}

Để tăng tính rõ ràng, sơ đồ lớp được tách thành hai nhóm: nhóm Core phụ trách tài khoản và vận hành nền tảng; nhóm IELTS phụ trách toàn bộ nội dung và tiến độ học tập~\cite{fowler2002patterns}.

Nhóm Core tập trung vào quản lý tài khoản và xác thực. Lớp User là trung tâm, lưu các thông tin cơ bản như email, mật khẩu (mã hoá bằng bcrypt), họ tên, avatar và vai trò (STUDENT / INSTRUCTOR / ADMIN). Người dùng đăng nhập bằng Google OAuth sẽ không có trường mật khẩu. Danh tính được xác thực qua JWT. Lớp Subscription liên kết 1-1 với User để quản lý gói cước hiện tại (Free / Premium / Pro) và ngày hết hạn.

Nhóm IELTS chứa toàn bộ nội dung học tập, tổ chức theo từng giai đoạn trong lộ trình~\cite{fowler2002patterns}. Ở giai đoạn Basic, bốn kỹ năng được tách thành bốn lớp exercise riêng biệt (Listening, Reading, Writing, Speaking) kèm bảng tiến độ. Giai đoạn Advanced bổ sung thêm lớp Session để lưu lại từng lần luyện tập cùng điểm số phản hồi từ AI. Giai đoạn Intensive dùng lớp IeltsIntensiveSession để quản lý phiên thi thử với vòng đời trạng thái từ khi bắt đầu cho đến khi có kết quả chấm điểm. Ngoài ra, phân hệ Vocab Lab dùng lớp Flashcard với thuật toán nhắc lại FSRS (qua thư viện ts-fsrs) để giúp người học ghi nhớ từ vựng theo lịch tối ưu.

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth]{4.1. Class Diagram/2-class-core.png}
    \caption{Sơ đồ lớp nhóm Core}
    \label{fig:class-user}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth]{4.1. Class Diagram/1-class-ielts.png}
    \caption{Sơ đồ lớp nhóm IELTS}
    \label{fig:class-learning}
\end{figure}

\section{Sơ đồ cơ sở dữ liệu}

Hệ thống dùng PostgreSQL~\cite{postgresql-docs} thông qua Prisma ORM~\cite{prisma-docs}. Cơ sở dữ liệu được lưu trữ trên Supabase — một dịch vụ PostgreSQL được quản lý sẵn, kết nối qua PgBouncer (port 6543) trong môi trường chạy thực và kết nối trực tiếp (port 5432) khi chạy migration. Toàn bộ schema được định nghĩa tập trung trong file schema.prisma với 66 model. Tương tự sơ đồ lớp, ERD cũng được tách thành hai nhóm để dễ đọc: nhóm Core và nhóm IELTS.

Nhóm Core quản lý người dùng và các nghiệp vụ liên quan. Bảng users lưu thông tin tài khoản, trạng thái hoạt động và định danh Google OAuth. Kèm theo là các bảng subscriptions, pricing\_plans (gói cước), payments (thanh toán), notifications (thông báo) và student\_teacher\_links (liên kết học viên -- giáo viên). Sơ đồ ERD nhóm Core được trình bày trong hình~\ref{fig:erd-core}.

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth]{4.2. ERD/2-erd-core.png}
    \caption{Sơ đồ ERD nhóm Core (PostgreSQL)}
    \label{fig:erd-core}
\end{figure}

Nhóm IELTS chứa toàn bộ dữ liệu học thuật, được tổ chức theo bốn giai đoạn của lộ trình. Mỗi giai đoạn có nhóm bảng riêng:

\begin{itemize}
    \item \textbf{Foundation:} Bộ ba nhóm bảng từ vựng nền, ngữ pháp và phát âm. Mỗi nhóm có cấu trúc phân cấp từ cuốn sách, chương, đơn vị bài tập cho đến bảng ghi nhận tiến độ của từng người học.
    \item \textbf{Basic:} Bốn bảng bài tập tương ứng với bốn kỹ năng Listening, Reading, Writing, Speaking, cùng bảng tiến độ để theo dõi kết quả.
    \item \textbf{Advanced:} Mỗi kỹ năng có thêm bảng Session lưu lại từng lần luyện tập kèm điểm phản hồi từ AI.
    \item \textbf{Intensive:} Ba bảng chính là exams (đề thi), exam\_sessions (phiên thi với vòng đời trạng thái) và results (kết quả chấm điểm AI sau khi xử lý).
\end{itemize}

Ngoài ra, phân hệ Vocab Lab dùng các bảng decks, flashcards và flashcard\_reviews để lưu trạng thái FSRS của từng thẻ. Các bảng Dictation, Shadowing và lộ trình học cũng được bổ sung để phục vụ các tính năng còn lại. Khoá ngoại được thiết lập với ON DELETE CASCADE ở toàn bộ bảng con nhằm giữ tính toàn vẹn dữ liệu khi xoá bản ghi cha. Sơ đồ ERD nhóm IELTS được trình bày trong hình~\ref{fig:erd-learning}.

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth]{4.2. ERD/1-erd-ielts.png}
    \caption{Sơ đồ ERD nhóm IELTS (PostgreSQL)}
    \label{fig:erd-learning}
\end{figure}

\section{Sơ đồ kiến trúc phần mềm}

Hệ thống được xây dựng theo kiến trúc event-driven modular monolith~\cite{newman2015microservices}, gồm các thành phần chính như sau:

\begin{itemize}
    \item \textbf{NestJS Backend Core:} Toàn bộ nghiệp vụ được tổ chức thành 18 module trong một ứng dụng NestJS 10 duy nhất. Các module giao tiếp nội bộ qua Dependency Injection, giúp dễ bảo trì mà không cần phân tán thành nhiều service riêng lẻ.
    \item \textbf{FastAPI AI Worker:} Một microservice Python độc lập, chịu trách nhiệm cho toàn bộ tác vụ AI. Worker này nhận công việc từ NestJS qua hàng đợi RabbitMQ (CloudAMQP), gọi Gemini API để chấm điểm, rồi ghi kết quả trực tiếp vào PostgreSQL qua psycopg2 bằng câu truy vấn SQL thô.
    \item \textbf{Tầng dữ liệu:} PostgreSQL trên Supabase (kết nối qua PgBouncer), Redis trên Upstash (cache và phiên làm việc), Cloudinary lưu trữ ảnh và file audio ghi âm của người dùng, và Google Cloud Storage phục vụ lưu trữ media tĩnh cho AI Worker khi chấm điểm.
    \item \textbf{Client:} Ứng dụng web Next.js và ứng dụng mobile React Native, đều kết nối với Backend Core qua HTTPS thông qua nginx.
\end{itemize}

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth]{4.3. Architecture Diagram/architecture-diagram.png}
    \caption{Sơ đồ kiến trúc phần mềm hệ thống IELTS Master English (Lexon)}
    \label{fig:architecture}
\end{figure}

\section{Sơ đồ luồng màn hình}

\subsection{Sơ đồ luồng màn hình website}

Trên nền tảng web, người dùng bắt đầu từ trang chủ landing page, đăng nhập hoặc đăng ký tài khoản, rồi vào dashboard chính. Tại đây, người học theo lộ trình từ Foundation (xây dựng nền tảng từ vựng, ngữ pháp, phát âm), qua Basic (làm quen bốn kỹ năng cơ bản), Advanced (luyện chuyên sâu với phản hồi từ AI), đến Intensive (thi thử sát thực tế). Ngoài ra còn có các công cụ bổ trợ như Vocab Lab (sổ tay từ vựng cá nhân), Dictation, Shadowing, Community, Roadmap và trang Thống kê để theo dõi tiến độ. Người quản lý có nhánh riêng vào admin dashboard với các chức năng quản lý nội dung, đề thi và tài khoản học viên.

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth]{4.4. Flow Chart/1-flowchart-web.png}
    \caption{Sơ đồ luồng màn hình website (Next.js)}
    \label{fig:flow-web}
\end{figure}

\subsection{Sơ đồ luồng màn hình mobile}

Trên nền tảng di động (mobile) (React Native), cấu trúc điều hướng sử dụng tab navigation ở tầng dưới cùng với 5 tab hiển thị: Home (Trang chủ), Explore (Khám phá), IELTS (trung tâm học tập), Community (Cộng đồng) và Profile (Hồ sơ). Các màn hình chuyên biệt như Vocab Lab, Shadowing, Pronunciation, Vocabulary, Grammar và More được truy cập qua điều hướng nội bộ từ tab IELTS và Home, không hiển thị trực tiếp trên thanh tab. Các màn hình con được quản lý bằng stack navigator tích hợp trong Expo Router.

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth]{4.4. Flow Chart/2-flowchart-mobile.png}
    \caption{Sơ đồ luồng màn hình mobile (React Native)}
    \label{fig:flow-mobile}
\end{figure}

\section{Giao diện chương trình}

Phần này trình bày 13 giao diện tiêu biểu của hệ thống trên nền tảng web, sắp xếp theo hành trình người dùng từ khi lần đầu truy cập cho đến khi sử dụng các tính năng học tập chuyên sâu.

\subsection{Giao diện trang chủ (landing page)}

Trang chủ là màn hình đầu tiên người dùng nhìn thấy khi truy cập hệ thống. Giao diện truyền tải thông điệp \textit{``Master English, Ace IELTS, Smarter with AI''} qua bố cục hiện đại với hình ảnh minh hoạ sinh động và hiệu ứng hoạt hình nhẹ. Nút ``START LEARNING'' đặt nổi bật ở trung tâm để khuyến khích người dùng bắt đầu ngay. Khi nhấn vào đó, hệ thống đưa người dùng thẳng vào phân hệ học tập chính. Giao diện trang chủ được trình bày trong hình~\ref{fig:ui-landing}.

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth]{4.5. Program Interface/images/01-homepage.png}
    \caption{Giao diện trang chủ (landing page)}
    \label{fig:ui-landing}
\end{figure}

\subsection{Giao diện dashboard người học}

IELTS dashboard là hub điều phối chính của người học. Mọi tiến độ luyện thi đều quy tụ về đây. Trục xương sống của trang là Learning Roadmap chia thành bốn giai đoạn tăng dần: Foundation (xây nền từ vựng, ngữ pháp, phát âm), Basic (nắm bốn kỹ năng cơ bản), Advanced (luyện sâu từng dạng câu hỏi với AI) và Intensive (thi thử sát thực tế). Bên cạnh thanh tiến độ và Quick Stats tổng quan, dashboard còn là điểm truy cập tới các công cụ bổ trợ (Tools \& Features) quan trọng: lộ trình học cá nhân hóa, Band Score Calculator, liên kết Student/Teacher để giáo viên theo dõi tiến độ học viên, cùng hệ thống Statistics chi tiết --- giúp người học khoanh trúng kỹ năng còn yếu. Giao diện dashboard được trình bày trong hình~\ref{fig:ui-dashboard}.

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth]{4.5. Program Interface/images/02-ielts-dashboard.png}
    \caption{Giao diện dashboard người học}
    \label{fig:ui-dashboard}
\end{figure}

\subsection{Giao diện Lộ trình học IELTS (Roadmap)}

Trang IELTS Roadmap là công cụ hỗ trợ xây dựng lộ trình học tập cá nhân hóa. Dựa trên kết quả Placement Test, mục tiêu Band Score và lịch học của người dùng, hệ thống tự động tạo lộ trình ôn luyện chi tiết, ưu tiên các kỹ năng còn yếu và bỏ qua những kỹ năng đã đạt ngưỡng thành thạo. Người học theo dõi tiến độ hoàn thành từng bước trực tiếp trên trang này; trạng thái từng bước được cập nhật tự động khi bài tập tương ứng được hoàn thành. Giao diện Roadmap được trình bày trong hình~\ref{fig:ui-roadmap}.

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth]{4.5. Program Interface/images/09-ielts-roadmap.png}
    \caption{Giao diện Lộ trình học IELTS cá nhân hoá (Roadmap)}
    \label{fig:ui-roadmap}
\end{figure}

\subsection{Giao diện thư viện Từ vựng (Foundation Vocabulary)}

Trang Vocabulary thuộc giai đoạn Foundation, mở thư viện sách từ vựng cho học viên xây vốn từ cơ bản. Trang hiển thị danh sách các Foundation Vocab Book theo chủ đề --- mỗi book kèm hình minh họa, tên chủ đề và tổng số từ. Người học duyệt qua các chủ đề, nhấn ``START LEARNING'' để vào màn hình học chi tiết --- bước đệm cần thiết trước khi đi vào luyện thi chuyên sâu. Giao diện thư viện Vocabulary được trình bày trong hình~\ref{fig:ui-vocabulary}.

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth]{4.5. Program Interface/images/03-vocabulary.png}
    \caption{Giao diện thư viện Từ vựng giai đoạn Foundation}
    \label{fig:ui-vocabulary}
\end{figure}

\subsection{Giao diện luyện Listening cơ bản (Basic Listening)}

Trang IELTS Basic Listening Exercise là giao diện làm bài tập thực hành kỹ năng nghe ở giai đoạn cơ bản. Trải nghiệm người dùng ở đây tập trung vào việc nghe audio và tương tác trả lời câu hỏi theo các định dạng bài thi quen thuộc (như điền từ, trắc nghiệm). Sau khi nộp bài, hệ thống sẽ tự động chấm điểm và cung cấp phản hồi (đáp án đúng, transcript) ngay lập tức để học viên rút kinh nghiệm và củng cố kỹ năng nghe cho từng dạng bài cụ thể. Giao diện luyện Listening cơ bản được trình bày trong hình~\ref{fig:ui-listening-basic}.

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth]{4.5. Program Interface/images/04-ielts-basic-listening-exercise.png}
    \caption{Giao diện luyện Listening giai đoạn Basic}
    \label{fig:ui-listening-basic}
\end{figure}

\subsection{Giao diện thi thử Reading (Intensive Reading)}

Trang IELTS Intensive Reading Test Taken mô phỏng phòng thi Reading sát với đề thực tế. Giao diện chia hai cột: cột trái là Passages học thuật, cột phải là danh sách câu hỏi cần trả lời. Trang khai thác hai cơ chế chính để tái hiện áp lực thi thật --- đồng hồ đếm ngược ép nhịp độ làm bài, và khả năng tương tác trực tiếp với văn bản (highlight, note). Giao diện thi thử Reading được trình bày trong hình~\ref{fig:ui-reading-intensive}.

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth]{4.5. Program Interface/images/05-ielts-intensive-reading-test-taken.png}
    \caption{Giao diện thi thử Reading (Intensive Reading Test)}
    \label{fig:ui-reading-intensive}
\end{figure}

\subsection{Giao diện trang đáp án Listening (Intensive Listening Answer)}

Trang IELTS Intensive Listening Answer Page hiển thị kết quả chi tiết ngay sau bài thi thử Listening. Báo cáo điểm số chia hai phần: tổng điểm thô và Band Score dự kiến theo thang quy đổi IELTS. Bên dưới là danh sách từng câu hỏi đặt cạnh đáp án người học chọn và đáp án chuẩn để đối chiếu. Audio Transcript đính kèm bôi đậm đoạn chứa câu trả lời --- người học dò ngay được mình nghe sai ở chỗ nào, tiếp tục cải thiện kỹ năng nghe hiểu. Giao diện trang đáp án Listening được trình bày trong hình~\ref{fig:ui-listening-answer}.

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth]{4.5. Program Interface/images/06-ielts-intensive-listening-answer-page.png}
    \caption{Giao diện trang đáp án Listening sau bài thi thử}
    \label{fig:ui-listening-answer}
\end{figure}

\subsection{Giao diện thi thử Speaking (Intensive Speaking)}

Trang IELTS Intensive Speaking Test Taken mô phỏng lại một phòng thi Speaking thực tế với sự hỗ trợ của AI. Người học sẽ tương tác với một ``giám khảo AI'', lắng nghe các câu hỏi thuộc Part 1, Part 2, Part 3 và sử dụng microphone để thu âm câu trả lời của mình trong thời gian quy định. Giao diện được thiết kế tối giản để người dùng tập trung vào việc nói, đồng thời có các chỉ báo thu âm rõ ràng để đảm bảo quá trình thi diễn ra mượt mà trước khi hệ thống gửi file âm thanh đi chấm điểm. Giao diện thi thử Speaking được trình bày trong hình~\ref{fig:ui-speaking}.

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth]{4.5. Program Interface/images/07-ielts-intensive-speaking-test-taken.png}
    \caption{Giao diện thi thử Speaking với giám khảo AI}
    \label{fig:ui-speaking}
\end{figure}

\subsection{Giao diện kết quả Writing với phản hồi AI (Intensive Writing Answer)}

Trang IELTS Intensive Writing Answer Page hiển thị kết quả đánh giá bài Writing sau khi pipeline AI hoàn tất. Giao diện gói trọn ba lớp thông tin: Band Score dự kiến ở trên cùng, nhận xét tổng quan ở giữa, và phân tích chi tiết theo bốn tiêu chí IELTS Writing (Task Achievement, Coherence, Lexical Resource, Grammatical Range) ở dưới. Phần thú vị nhất là danh sách lỗi ngôn ngữ cụ thể được AI bóc ra --- mỗi lỗi kèm câu gốc, phiên bản đã sửa và giải thích ngắn, giúp người học chốt được điểm yếu cụ thể về ngữ pháp và từ vựng. Giao diện kết quả Writing được trình bày trong hình~\ref{fig:ui-writing-answer}.

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth]{4.5. Program Interface/images/08-ielts-intensive-writing-answer-page.png}
    \caption{Giao diện kết quả Writing với phản hồi chi tiết của AI}
    \label{fig:ui-writing-answer}
\end{figure}

\subsection{Giao diện luyện nghe chép chính tả (Dictation)}

Trang Dictation Lessons (Luyện nghe chép chính tả) là một phân hệ luyện tập chuyên biệt giúp nâng cao sự nhạy bén của kỹ năng nghe. Tại đây, người dùng có thể nghe các đoạn audio ngắn và thực hành gõ lại toàn bộ nội dung (Dictation). Giao diện chú trọng công cụ đối chiếu độ chính xác giữa bài làm của người dùng với transcript gốc. Giao diện luyện Dictation được trình bày trong hình~\ref{fig:ui-dictation}.

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth]{4.5. Program Interface/images/10-dictation-lessons.png}
    \caption{Giao diện luyện nghe chép chính tả (Dictation)}
    \label{fig:ui-dictation}
\end{figure}

\subsection{Giao diện sổ tay từ vựng cá nhân (Vocab Lab)}

Vocab Lab là sổ tay từ vựng kỹ thuật số cá nhân --- một ``phòng thí nghiệm'' nơi người học lưu, quản lý và ôn lại các từ đã thu thập trong suốt quá trình học trên hệ thống. Trang cho phép tạo deck tùy chỉnh, ôn qua flashcard và lên lịch ôn tự động bằng thuật toán Spaced Repetition. Cơ chế này đảm bảo từ vựng được ôn đúng thời điểm não sắp quên --- ghi nhớ lâu dài với chi phí thời gian tối thiểu. Giao diện Vocab Lab được trình bày trong hình~\ref{fig:ui-vocablab}.

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth]{4.5. Program Interface/images/11-vocab-lab.png}
    \caption{Giao diện sổ tay từ vựng cá nhân (Vocab Lab)}
    \label{fig:ui-vocablab}
\end{figure}

\subsection{Giao diện Cộng đồng (Community)}

Trang Community là không gian kết nối xã hội nội bộ giữa người dùng trên toàn hệ thống. Học viên đăng bài chia sẻ mẹo học, thảo luận kinh nghiệm ôn thi, khoe band điểm vừa đạt và tương tác qua Like/Comment/Bookmark với các thành viên khác. Vai trò chính của tính năng này: tạo môi trường học tập mang tính cộng đồng, gia cố động lực và sự gắn kết của người học với nền tảng. Giao diện Community được trình bày trong hình~\ref{fig:ui-community}.

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth]{4.5. Program Interface/images/12-community.png}
    \caption{Giao diện Cộng đồng (Community)}
    \label{fig:ui-community}
\end{figure}

\subsection{Tính năng tra từ và lưu vào Vocab Lab bằng AI}

Tính năng Add to Vocab Lab with AI là một panel nổi xuất hiện khi người học bôi đen từ hoặc cụm từ trên các màn hình học hỗ trợ (Foundation Vocabulary, IELTS Basic, Dictation). Panel cung cấp hai tùy chọn: \textit{``Add to Vocab''} điền sẵn từ và ngữ cảnh vào form tạo thẻ trong tab Add của Vocab Lab để người học bổ sung nội dung còn lại và lưu; \textit{``Ask AI''} mở Chat AI để trợ lý tự động sinh đầy đủ nội dung cho thẻ theo Card Type đang dùng. Người học có thể xem lại và chỉnh sửa trước khi lưu mà không cần rời khỏi trang đang học. Giao diện tính năng được trình bày trong hình~\ref{fig:ui-add-vocablab-ai}.

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth]{4.5. Program Interface/images/13-add-to-vocab-lab-with-ai.png}
    \caption{Pop-up tra từ và lưu vào Vocab Lab bằng AI}
    \label{fig:ui-add-vocablab-ai}
\end{figure}

\section{Kiểm thử hệ thống}

Tầng backend-core của hệ thống được kiểm thử tự động với hai mục tiêu: phát hiện sớm regression sau mỗi commit và làm tài liệu sống mô tả hành vi mong đợi của từng mô-đun. Toàn bộ test case được tổ chức theo tháp kiểm thử ba tầng (unit, integration và end-to-end), thực thi tự động trong pipeline CI và sinh báo cáo Markdown để đối chiếu trực tiếp với báo cáo khóa luận này.

\subsection{Chiến lược và công cụ kiểm thử}

Khung kiểm thử chính là Jest 29.7.0 kết hợp Supertest cho các yêu cầu HTTP, ts-jest cho mã TypeScript và jest-junit để xuất kết quả ở định dạng JUnit phục vụ pipeline CI. Tầng unit cô lập từng service bằng cách mock toàn bộ phụ thuộc (PrismaService, JwtService, Redis, RabbitMQ) thông qua Dependency Injection của NestJS, qua đó chỉ kiểm chứng logic nghiệp vụ thuần. Tầng integration mock các phụ thuộc ngoài nhưng giữ nguyên Controller, Pipe, Guard và Interceptor bằng cách khởi tạo INestApplication thực; nhờ đó test xác minh được toàn bộ vòng đời request và response, bao gồm cả ValidationPipe, JwtAuthGuard và RolesGuard.

Tầng end-to-end (E2E) chạy độc lập bằng lệnh npm run test:e2e với cấu hình riêng jest-e2e.json. Khác với hai tầng trên, E2E sử dụng cơ sở dữ liệu PostgreSQL thật trên schema test thay vì mock, qua đó kiểm chứng được cả ràng buộc cấp cơ sở dữ liệu (chỉ mục duy nhất trên email, khóa ngoại) và hành vi stateless của JWT. Bộ E2E hiện gồm 33 test case nằm trong hai file spec. File auth.e2e-spec.ts (12 test case) mô phỏng toàn bộ luồng xác thực: đăng ký, đăng nhập, truy cập tài nguyên bảo vệ và refresh token, kèm các kịch bản tiêu cực như token hết hạn, token bị giả mạo chữ ký và đăng ký trùng email. File admin-ielts.e2e-spec.ts (21 test case) là bộ ``golden test'' cho luồng quản trị nội dung IELTS: kiểm tra quy tắc hợp lệ hóa đề thi (assertGraderCompatible), chặn trùng lặp khi commit nội dung từ vùng staging, gộp nhóm câu hỏi cho bài FULL\_\allowbreak{}TEST, chuẩn hóa đáp án (parseIELTSAnswer) và chấm điểm đối chiếu với kết quả vàng (golden) cho hai kỹ năng Listening và Reading.

Để duy trì chất lượng mã nguồn theo thời gian, dự án áp dụng ngưỡng độ phủ tối thiểu (coverage threshold) trên các service trọng yếu, được khai báo trong tệp jest.config.ts. Khi lệnh npm run test:cov chạy trong pipeline CI, Jest trả về exit code khác 0 nếu coverage giảm xuống dưới ngưỡng, buộc CI fail và chặn merge pull request. Các ngưỡng hiện hành được trình bày trong bảng~\ref{tab:coverage_thresholds}.

% 3 cột p{}, divisor=100: 50+25+25; -4pt buffer cho rounding
\setlength{\tbu}{\dimexpr(\linewidth - 6\tabcolsep - 4\arrayrulewidth - 4pt)/100\relax}
\begin{longtable}{|p{50\tbu}|p{25\tbu}|p{25\tbu}|}
    \caption{Ngưỡng độ phủ mã (coverage threshold) backend-core}
    \label{tab:coverage_thresholds} \\
    \hline
    \textbf{Mô-đun / tệp nguồn} & \textbf{Lines tối thiểu (\%)} & \textbf{Branches tối thiểu (\%)} \\ \hline
    \endfirsthead
    \multicolumn{3}{r}{\textit{(tiếp trang trước)}} \\
    \hline
    \textbf{Mô-đun / tệp nguồn} & \textbf{Lines tối thiểu (\%)} & \textbf{Branches tối thiểu (\%)} \\ \hline
    \endhead
    \hline \multicolumn{3}{r}{\textit{(tiếp trang sau)}} \\
    \endfoot
    \hline
    \endlastfoot

    auth.service.ts (xác thực và phân quyền) & 70 & 60 \\ \hline
    subscriptions.service.ts (lifecycle gói Premium và thanh toán VNPay) & 50 & 30 \\ \hline
    gamification.service.ts (điểm thưởng và thành tích) & 30 & 10 \\ \hline
    Toàn dự án (ngưỡng tối thiểu chung) & 28 & 9 \\ \hline
\end{longtable}

Mỗi lần thực thi npm test hoặc npm run test:report, một custom Jest reporter đặt tại test/reporters/markdown-reporter.js tự động sinh tài liệu docs/testing/test-results.md, ghi nhận đầy đủ ID, mô tả, trạng thái và thời gian thực thi của từng test case. Tài liệu này là nguồn dữ liệu tham chiếu cho các bảng kết quả ở các mục tiếp theo.

\subsection{Phạm vi kiểm thử}

Ba mươi lăm file spec của backend-core được tổ chức thành 18 nhóm phù hợp với cấu trúc mô-đun của hệ thống. Mười bảy nhóm đầu thuộc tầng unit và integration (33 file, tổng 255 test case); nhóm thứ mười tám là bộ E2E gồm 33 test case chạy trên cơ sở dữ liệu PostgreSQL thật. Bảng~\ref{tab:test_scope} liệt kê chi tiết. Cột File spec hiển thị tên file rút gọn; đường dẫn đầy đủ với specs mô-đun là backend-core/src/modules/<module>/tests/, còn các bộ test dùng chung nằm dưới backend-core/test/.

% 5 cột p{}, divisor=100: 4+18+32+6+40; -4pt buffer cho rounding
\setlength{\tbu}{\dimexpr(\linewidth - 10\tabcolsep - 6\arrayrulewidth - 4pt)/100\relax}
{\small
\begin{longtable}{|p{4\tbu}|p{18\tbu}|p{32\tbu}|p{6\tbu}|p{40\tbu}|}
    \caption{Phạm vi kiểm thử backend-core}
    \label{tab:test_scope} \\
    \hline
    \textbf{STT} & \textbf{Nhóm chức năng} & \textbf{File spec} & \textbf{Số TC} & \textbf{Trọng tâm kiểm thử} \\ \hline
    \endfirsthead
    \multicolumn{5}{r}{\textit{(tiếp trang trước)}} \\
    \hline
    \textbf{STT} & \textbf{Nhóm chức năng} & \textbf{File spec} & \textbf{Số TC} & \textbf{Trọng tâm kiểm thử} \\ \hline
    \endhead
    \hline \multicolumn{5}{r}{\textit{(tiếp trang sau)}} \\
    \endfoot
    \hline
    \endlastfoot

    1 & Xác thực và phân quyền (auth) & register.spec.ts\newline login.spec.ts\newline auth-extended.spec.ts\newline jwt-strategy.spec.ts & 29 & Đăng ký với DTO validation và bcrypt hash; đăng nhập + cấp JWT; refresh token; đổi mật khẩu; Google OAuth ID token; JwtStrategy của Passport \\ \hline

    2 & Tài khoản người dùng (users) & update-profile.spec.ts\newline users.service.spec.ts & 36 & Tầng controller: DTO whitelist (forbid\allowbreak{}Non\allowbreak{}Whitelisted), email uniqueness, role enum, trạng thái hoạt động. Tầng service: CRUD hồ sơ, liên kết học viên--giáo viên, thống kê học viên, đăng ký push token, theo dõi hoạt động và gợi ý học tập \\ \hline

    3 & Cộng đồng -- đăng bài (posts) & posts.spec.ts & 11 & CRUD bài đăng, like/unlike, ẩn bài đã xóa, kích hoạt gamification onEvent (XP = 5) \\ \hline

    4 & Thông báo (notifications) & notifications.spec.ts & 9 & Phân trang, ownership filter, đánh dấu tất cả đã đọc, xóa thông báo \\ \hline

    5 & Luyện phát âm IPA (pronunciation) & pronunciation.spec.ts\newline pronunciation.service.spec.ts & 21 & CRUD âm vị (RolesGuard chỉ ADMIN), Redis cache và invalidation, đánh giá mức độ thành thạo IPA, vòng đời attempt từ PENDING đến hoàn tất \\ \hline

    6 & IELTS -- thống kê, advanced, chuỗi học (ielts) & ielts-statistics.spec.ts\newline ielts-advanced.service.spec.ts\newline streak.service.spec.ts & 45 & Thống kê theo từng giai đoạn (Foundation\allowbreak{}/Basic\allowbreak{}/Advanced\allowbreak{}/Intensive), vòng đời L/R/W/S session, đánh giá đáp án, dispatch tác vụ AI qua RabbitMQ, chuỗi học liên tục và mốc thành tích \\ \hline

    7 & Luyện nghe chép chính tả (dictation) & dictation-progress.service.spec.ts & 6 & Tiến độ upsert, sentence XP, lesson completion notification, không award trùng \\ \hline

    8 & Luyện shadowing video (shadowing) & shadowing-progress.service.spec.ts & 6 & Tiến độ upsert, sentence XP, hoàn thành bài khi đủ số câu \\ \hline

    9 & Vocab Lab -- lặp lại ngắt quãng (vocab-lab) & vocab-lab-fsrs.service.spec.ts & 10 & Thuật toán FSRS với các mức rating (Good/Again/Easy), lịch ôn theo trường due, ownership deck \\ \hline

    10 & Gamification -- XP và thành tích (gamification) & gamification.service.spec.ts & 10 & Tạo XpLog, cập nhật totalXp, thông báo lên level, grant achievement, không tạo trùng \\ \hline

    11 & Bài thi thử IELTS Intensive (exams) & exams-submit.spec.ts\newline result-callback.spec.ts & 10 & Submit + dispatch publishGradingTask, quota guard cho gói Free, HMAC signature callback từ AI worker, idempotency \\ \hline

    12 & Polling kết quả chấm AI (results) & results.spec.ts & 6 & Trạng thái GRADING/GRADED/GRADING\_\allowbreak{}FAILED; bảo vệ IDOR (trả 404 ẩn existence) \\ \hline

    13 & Subscription và thanh toán (subscriptions) & subscriptions-webhook.spec.ts\newline subscriptions.service.spec.ts\newline subscriptions.cron.spec.ts & 13 & VNPay IPN HMAC-SHA512, replay protection, dùng thử 7 ngày, cancel subscription, quota usage và rollback, downgrade cron sau grace period 3 ngày \\ \hline

    14 & Edge case -- phân trang và giá trị biên (test/edge-cases) & pagination.spec.ts\newline boundary-values.spec.ts & 16 & Query param bất thường (page = 0/-1, limit cực lớn, cursor không tồn tại), điểm 0/100/101, body rỗng, mảng imageUrls quá kích thước \\ \hline

    15 & Bảo mật -- truy cập chéo người dùng (test/security) & cross-user-access.spec.ts & 8 & IDOR trên VocabLab/Notes/Posts (404 vs 403 tùy mô hình dữ liệu), khe hở admin bypass đã được ghi nhận \\ \hline

    16 & Ghi chú theo câu hỏi (notes) & notes.service.spec.ts\newline notes.controller.spec.ts\newline notes.module.spec.ts & 8 & Lấy/ghi/xóa ghi chú gắn với bài thi (getExamNotes, upsertNote, deleteNote), lọc theo chủ sở hữu, wiring module và controller \\ \hline

    17 & Hạ tầng dùng chung (common, app) & app.controller.spec.ts\newline app.service.spec.ts\newline roles.guard.spec.ts\newline prisma.service.spec.ts & 11 & Health check và metadata ứng dụng, RolesGuard phân quyền theo decorator @Roles (cho phép khi khớp role, ném ForbiddenException khi không khớp), vòng đời kết nối PrismaService \\ \hline

    18 & E2E -- xác thực và quản trị IELTS (test/e2e) & auth.e2e-spec.ts\newline admin-ielts.e2e-spec.ts & 33 & Postgres thật trên schema test. Luồng register/login/refresh kèm token hết hạn, token giả mạo chữ ký, đăng ký trùng email (12 TC); golden test cho luồng quản trị nội dung IELTS: hợp lệ hóa đề, chống trùng commit, gộp nhóm câu hỏi, chuẩn hóa và chấm điểm đối chiếu Listening/Reading (21 TC) \\ \hline
\end{longtable}
}

Hai nhóm Edge case (STT~14) và Security (STT~15) không gắn với một mô-đun đơn lẻ mà kiểm thử xuyên suốt nhiều endpoint, tập trung vào các loại lỗi thường gặp như IDOR (Insecure Direct Object Reference), kiểm tra giá trị biên và phân trang bất thường. Nhóm Subscription (STT~13) bao gồm cả webhook IPN của cổng thanh toán VNPay (một luồng tích hợp với hệ thống bên ngoài) nên được tách thành ba file spec để phân tách trách nhiệm: kiểm thử webhook, lifecycle service và cron downgrade.

\subsection{Kết quả kiểm thử}

Sau khi hoàn thiện hệ thống, toàn bộ 255 test case unit/integration cùng 33 test case E2E --- tổng cộng 288 test case --- được thực thi tự động qua hai lệnh: npm test (cho unit/integration, đồng thời kích hoạt custom reporter sinh báo cáo Markdown) và npm run test:e2e (cho E2E). Cả hai lệnh đều chạy trên Jest và ghi log chi tiết per-test vào tài liệu auto-generated docs/testing/test-results.md. Ở lần chạy gần nhất (04/06/2026), toàn bộ 255 test case unit/integration thuộc 33 test suite đều đạt trạng thái \textbf{Pass} sau khoảng 8,4 giây; 33 test case E2E (chạy trên PostgreSQL schema test) cũng đạt \textbf{Pass} hoàn toàn sau khoảng 5,4 giây --- đạt tỷ lệ thành công 288/288 (100\%). Bảng~\ref{tab:testcase_result} tổng hợp kết quả theo từng nhóm chức năng đã định nghĩa ở phạm vi kiểm thử; chi tiết per-test có thể đối chiếu trong tài liệu docs/testing/test-results.md đính kèm với mã nguồn.

\renewcommand{\arraystretch}{1.1}
% 8 cột p{}, divisor=100: 4+28+8+8+8+10+16+18; -4pt buffer cho rounding
\setlength{\tbu}{\dimexpr(\linewidth - 16\tabcolsep - 9\arrayrulewidth - 4pt)/100\relax}
{\small
\begin{longtable}{|p{4\tbu}|p{28\tbu}|p{8\tbu}|p{8\tbu}|p{8\tbu}|p{10\tbu}|p{16\tbu}|p{18\tbu}|}
    \caption{Tổng hợp kết quả kiểm thử theo nhóm}
    \label{tab:testcase_result} \\
    \hline
    \textbf{STT} & \textbf{Nhóm chức năng} & \textbf{Số TC} & \textbf{Pass} & \textbf{Fail} & \textbf{Tỷ lệ} & \textbf{Người TH} & \textbf{Ngày} \\ \hline
    \endfirsthead
    \multicolumn{8}{r}{\textit{(tiếp trang trước)}} \\
    \hline
    \textbf{STT} & \textbf{Nhóm chức năng} & \textbf{Số TC} & \textbf{Pass} & \textbf{Fail} & \textbf{Tỷ lệ} & \textbf{Người TH} & \textbf{Ngày} \\ \hline
    \endhead
    \hline \multicolumn{8}{r}{\textit{(tiếp trang sau)}} \\
    \endfoot
    \hline
    \endlastfoot

    1 & Xác thực và phân quyền & 29 & 29 & 0 & 100\% & Auto (Jest) & 04/06/2026 \\ \hline
    2 & Tài khoản người dùng & 36 & 36 & 0 & 100\% & Auto (Jest) & 04/06/2026 \\ \hline
    3 & Cộng đồng -- đăng bài & 11 & 11 & 0 & 100\% & Auto (Jest) & 04/06/2026 \\ \hline
    4 & Thông báo & 9 & 9 & 0 & 100\% & Auto (Jest) & 04/06/2026 \\ \hline
    5 & Luyện phát âm IPA & 21 & 21 & 0 & 100\% & Auto (Jest) & 04/06/2026 \\ \hline
    6 & IELTS (thống kê, advanced, chuỗi học) & 45 & 45 & 0 & 100\% & Auto (Jest) & 04/06/2026 \\ \hline
    7 & Luyện nghe chép chính tả & 6 & 6 & 0 & 100\% & Auto (Jest) & 04/06/2026 \\ \hline
    8 & Luyện shadowing video & 6 & 6 & 0 & 100\% & Auto (Jest) & 04/06/2026 \\ \hline
    9 & Vocab Lab (lặp lại ngắt quãng) & 10 & 10 & 0 & 100\% & Auto (Jest) & 04/06/2026 \\ \hline
    10 & Gamification (XP và thành tích) & 10 & 10 & 0 & 100\% & Auto (Jest) & 04/06/2026 \\ \hline
    11 & Bài thi thử IELTS Intensive & 10 & 10 & 0 & 100\% & Auto (Jest) & 04/06/2026 \\ \hline
    12 & Polling kết quả chấm AI & 6 & 6 & 0 & 100\% & Auto (Jest) & 04/06/2026 \\ \hline
    13 & Subscription và thanh toán & 13 & 13 & 0 & 100\% & Auto (Jest) & 04/06/2026 \\ \hline
    14 & Edge case (phân trang, giá trị biên) & 16 & 16 & 0 & 100\% & Auto (Jest) & 04/06/2026 \\ \hline
    15 & Bảo mật (truy cập chéo người dùng) & 8 & 8 & 0 & 100\% & Auto (Jest) & 04/06/2026 \\ \hline
    16 & Ghi chú theo câu hỏi & 8 & 8 & 0 & 100\% & Auto (Jest) & 04/06/2026 \\ \hline
    17 & Hạ tầng dùng chung (common, app) & 11 & 11 & 0 & 100\% & Auto (Jest) & 04/06/2026 \\ \hline
    18 & E2E (xác thực + quản trị IELTS) & 33 & 33 & 0 & 100\% & Auto (Jest) & 04/06/2026 \\ \hline
    \multicolumn{2}{|r|}{\textbf{Tổng cộng}} & \textbf{288} & \textbf{288} & \textbf{0} & \textbf{100\%} & \multicolumn{2}{c|}{Auto (Jest)} \\ \hline
\end{longtable}
}

Kết quả khẳng định backend-core đáp ứng đầy đủ các yêu cầu nghiệp vụ đặc tả ở chương~3 --- ràng buộc bảo mật (IDOR, JWT lifecycle), tính nhất quán cấp cơ sở dữ liệu (unique email, ownership deck/note/post) và tính chính xác của các luồng tích hợp ngoài như VNPay IPN và AI grading callback đều giữ vững. Quá trình kiểm thử cũng chủ động ghi nhận năm khe hở nhỏ --- là các trường hợp hiện vẫn ``pass'' theo hành vi mặc định nhưng được đánh dấu để cải thiện ở phiên bản sau: (i) JwtStrategy.validate() trả về undefined cho trường sub hoặc role khi token thiếu các trường này, thay vì ném UnauthorizedException (TC\_JWT\_04, TC\_JWT\_05); (ii) điểm phát âm bằng 101 vẫn được chấp nhận do DTO thiếu ràng buộc @Max(100) (TC\_BOUND\_03); (iii) bài đăng kèm 11 ảnh vẫn được chấp nhận do thiếu @ArrayMaxSize(10) (TC\_BOUND\_08); và (iv) tài khoản quản trị chưa được phép xóa bài của người dùng khác --- hiện trả 403 thay vì 200 do cơ chế admin bypass chưa được hiện thực (TC\_SEC\_08). Nhật ký per-test của toàn bộ 288 test case được lưu trong tài liệu auto-generated docs/testing/test-results.md đính kèm với mã nguồn, phục vụ tra cứu và kiểm chứng.

