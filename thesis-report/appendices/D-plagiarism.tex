% !TEX root = ../main.tex
% ============================================================
% PHỤ LỤC D — KẾT QUẢ KIỂM TRA ĐẠO VĂN
% ============================================================
\chapter{Kết quả kiểm tra đạo văn}
\label{app:daovan}

Báo cáo đồ án đã được kiểm tra đạo văn qua hệ thống kiểm tra do nhà trường cung cấp. Kết quả kiểm tra cho thấy tỷ lệ trùng lặp nằm trong giới hạn cho phép theo quy định của Trường Đại học Công nghiệp TP. Hồ Chí Minh.

\begin{figure}[H]
    \centering
    % Chèn ảnh kết quả kiểm tra đạo văn tại đây
    \fbox{\parbox{\linewidth}{\centering\vspace{3cm}
    \textit{[Chèn ảnh screenshot kết quả kiểm tra đạo văn]}
    \vspace{3cm}}}
    \caption{Kết quả kiểm tra đạo văn của báo cáo đồ án}
    \label{fig:daovan}
\end{figure}

